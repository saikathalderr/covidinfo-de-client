import { Case } from '@/interfaces/cases.interface';

type CasesListProps = {
    cases: Case[];
};

function CasesList({ cases }: CasesListProps) {
    return <div>CasesList</div>;
}

export default CasesList;
